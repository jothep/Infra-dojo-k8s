apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx-versioned.fullname" . }}-test-connection"
  labels:
    {{- include "nginx-versioned.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginx-versioned.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
